FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 80
EXPOSE 443

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY ["materials-service.csproj", "./"]
RUN dotnet restore "materials-service.csproj"
COPY . .
RUN dotnet build "materials-service.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "materials-service.csproj" -c Release -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "materials-service.dll"]